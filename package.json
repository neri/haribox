{
  "name": "haribote",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate:initial-fs && vite",
    "generate:initial-fs": "node scripts/generate-initial-fs.mjs",
    "prebuild": "make wasm && npm run generate:initial-fs",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "latest",
    "vite": "latest"
  },
  "dependencies": {
    "encoding-japanese": "latest",
    "pako": "latest"
  }
}