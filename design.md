# HariboteBox 設計書

- アプリ名: HariboteBox
- 省略名: HariboteBox
- 役割: 「はりぼてOS」エミュレーター用のデスクトップ環境

## 1. 目的

Webブラウザ上で動作する SPA として、「はりぼてOS」というOSのアプリケーションを実行する環境をエミュレートする。本プロジェクトはこのエミュレーター環境の **ウィンドウシステムとシェルUI** の実装に主眼を置く。

### 1.1 アプリ全体の役割

- Webブラウザ内で「はりぼてOS」のOS環境をシミュレート
- Rust/Wasm で実装されたOS上のアプリケーションを実行
- ウィンドウベースのマルチタスク環境を提供

### 1.2 本プロジェクトの実装範囲

Webブラウザ内に以下のUIを実装する:

- **デスクトップ環境**: 画面全体をデスクトップとして表示
- **ウィンドウシステム**: 複数ウィンドウの生成、移動、前面化、個別クローズ
- **シェル（ターミナル）**: コマンド入力・実行・結果表示
- **ファイルシステム UI**: メモリ上の簡易ファイルシステムと localStorage 永続化
- **ファイル管理**: デスクトップへのドラッグ&ドロップによるファイル取り込み
- **タスク実行**: ターミナルコマンド経由で Worker 上の Rust(Wasm) タスクを実行

## 2. 適用範囲

本設計書は以下の実装を対象とする:

- **フロントエンド実装** (TypeScript + Vite)
  - UI レイアウトと状態管理
  - ウィンドウ管理ロジック
  - ターミナル（シェル）UI と コマンド解析
  - ファイルシステム UI
- **ローカルストレージ永続化**
  - ファイルシステムデータの圧縮・展開
- **Rust/Wasm タスク連携**
  - Web Worker を経由したタスク実行
  - JavaScript ↔ Rust インターフェース

本設計書は以下を対象外とする:

- Rust/Wasm実装（別プロジェクト）
- ターミナルコマンド処理の詳細実装（別ドキュメント）
- ようこそウィンドウの具体的なコンテンツテキスト（人間がメンテナンス）
- README 更新（人間が編集）

## 3. 前提技術

### 3.1 本プロジェクトのスタック

- 言語: TypeScript
- 実行環境: Webブラウザ (SPA)
- パッケージマネージャ: pnpm
- ビルドツール: Vite
- UI フレームワーク: (軽量VanillaJS / 別途定義)
- Worker: Web Worker
- 圧縮ライブラリ: pako (zlib 実装)
  - 初期ファイルシステムのビルド時圧縮
  - localStorage 永続化データの圧縮・展開

### 3.2 連携するコンポーネント

- **Rust/Wasm タスク**: Worker で実行される Rust コード（別プロジェクト）
  - wasm-bindgen (Rust ↔ JavaScript インターフェース)

## 4. 用語定義

### 4.1 システム構成

- **はりぼてOS**: メインのオペレーティングシステム（別プロジェクト）
- **エミュレーター**: Webブラウザ上の仮想環境。はりぼてOS のアプリを実行する
- **HDE (Haribote Desktop Environment)**: 本プロジェクト。エミュレーター上のウィンドウシステムとシェル UI

### 4.2 UI 要素

- デスクトップ: 画面全体の作業領域。デフォルト色は青緑。
- タスクバー: 画面下部の固定操作パネル。
- ウィンドウ: デスクトップ内に配置される可動要素。
- ウィンドウID: 各ウィンドウのユニークな識別子（UUIDv4 文字列形式）。複数インスタンス可能なウィンドウタイプでも個別に異なる ID を持つ。
- アプリID: ウィンドウタイプそのものの識別子（UUIDv4 文字列形式）。同じタイプのウィンドウは同じアプリID を共有。1 個限定ウィンドウの判定などに使用。
- Zオーダー: ウィンドウの前後関係。
- アクティブウィンドウ: キーボード入力フォーカスを持つウィンドウ。ウィンドウ内のサブアプリケーション（ターミナル、ファイルマネージャなど）は、アクティブなウィンドウのみがキーボード操作を受け付ける。ウィンドウをクリックするとそのウィンドウがアクティブ化される。
- デフォルト端末ウィンドウ: 宛先端末が曖昧なメッセージ表示に使用される端末。最初に開いた端末ウィンドウがデフォルトとなり、その後複数存在する場合も最初にデフォルトになったウィンドウが優先される。デフォルト端末を閉じた場合は次の端末候補を探す。端末が1個のみの場合はそのウィンドウがデフォルト。端末が存在しない場合はダミーウィンドウとして扱う（エラーにはならないがメッセージは表示されない）。

## 5. 画面構成

### 5.1 全体レイアウト

- 画面全体を 2 領域で構成する。
- 上側: デスクトップ領域 (可変高)
- 下側: タスクバー領域 (固定高)

### 5.2 デスクトップ領域

- 背景色: `#00a9a9` (Windows 95/98 風の明るいティール、CSS変数 `--desktop-bg` で定義)
- ウィンドウを複数重ねて表示
- ウィンドウはデスクトップ外にはみ出した位置でも描画可能

### 5.3 タスクバー領域

- 高さ: `48px` (下側固定)
- レイアウト: 左側 → 中央 → 右側の 3 領域で構成
  - 左側: ハンバーガーメニューボタン
  - 中央: ウィンドウボタングループ領域（タスクバーウィンドウ一覧表示）
  - 右側: ボリュームコントロール + 時計表示
- ウィンドウボタンは開いているウィンドウの情報をアイコン + タイトルで表示
- グループ化：複数の同一タイプのウィンドウは 1 つのグループボタンとして集約表示
  - ホバー時またはクリック時にドロップダウンで個別ウィンドウ一覧を表示
  - Rust Task (Canvas) は個別表示（グループ化しない）

### 5.4 スタートメニュー

タスクバー左端のハンバーガーメニューボタンを「スタートメニュー」と定義する。

- **スタートメニューボタン**:
  - アイコン: `src/assets/icons/box-multiple.svg`
  - ボタン押下でメニューポップアップが表示
  - メニューはボタンの上側に表示される
  - メニュー外部をクリック、または ESC キー押下でメニューをクローズ
  - メニューはウィンドウより前面に配置

- **スタートメニュー項目**:
  - 各項目には、起動するウィンドウと同じアイコンを表示する
  - ターミナル: `src/assets/icons/terminal-2.svg` 新規端末ウィンドウを作成
  - ファイル: `src/assets/icons/folder.svg` ファイルマネージャウィンドウを起動
  - About...: `src/assets/icons/category.svg` About ウィンドウを表示

### 5.5 ボリュームコントロール

タスクバー右側に配置される音量調整UI。デスクトップ内で実行されるRust/Wasm アプリケーションによる音声出力を制御する。

**基本構成:**
- ボリュームボタン: タスクバー右側に配置、ステータスアイコン表示
- ボリュームポップアップ: ボタン上に表示される音量調整パネル
  - 位置: ボタンの上側に表示（タスクバーの上方向）
  - ボリュームポップアップ外部をクリック、または ESC キー押下でクローズ
  - ウィンドウより前面に配置

**ボリュームボタン仕様:**
- 大きさ: 24×24px
- ボタン押下でポップアップの表示/非表示をトグル
- アイコン表示: 以下のいずれかに応じて選択

| 状態 | 表示アイコン | 説明 |
|------|-------------|------|
| AudioContext suspended | volume-3.svg (X) | AudioContext がまだアクティベーションされていない状態 |
| ボリューム 0% | volume-3.svg (X) | 完全ミュート状態 |
| ボリューム 1～29% | volume-4.svg (小) | 小さい音量 |
| ボリューム 30～69% | volume-2.svg (中) | 中程度の音量 |
| ボリューム 70～100% | volume.svg (大) | 大きい音量 |

**ボリュームポップアップ仕様:**
- 幅: 240px
- 構成: 上部に数値表示、下部にスライダーと音量アイコン
  - 数値表示: 現在のボリューム値を 0～100 の整数で表示（太字）
  - スライダー: HTML5 range input（0～100）
  - アイコン: スライダー両側にボリュームアイコン（視認性向上）

**ボリューム保存:**
- ボリューム変更時に `localStorage` キー `'haribote.audio.volume'` に 0.0～1.0 の実数値で保存
- アプリ起動時に localStorage から復元、保存されたボリュームで初期化
- localStorage にキーが存在しない場合は初期値 0.5 で初期化

**Web Audio API との連携:**
- 初回ユーザー操作時に AudioContext を `resume()` で実行状態に遷移（必須）
- ボリュームボタン押下でも AudioContext の resume を試行
- MMLファイル実行時にも AudioContext の resume を試行
- GainNode を介して全Oscillator のゲインを統一制御
- Oscillator 生成時は初期ゲイン 0.3 で制限（クリッピング防止）

### 5.6 タスクバーウィンドウボタン

**ウィンドウボタングループの表示**:
- タスクバー中央領域にウィンドウボタンを配置
- 各ボタンはウィンドウのアイコンとタイトルを表示
- ボタンサイズ: アイコン 16x16px + パディング + タイトルテキスト + パディング
- タイトルは必要に応じて短縮（画面幅に応じて制限）
- アクティブウィンドウのボタンは異なるスタイル（背景色など）で表示

**グループ化仕様**:

| ウィンドウタイプ | グループ化 | アイコン |
|------------------|-----------|----------|
| `terminal` | ○ グループ化 | terminal-2.svg |
| `filemanager` | ○ グループ化 | folder.svg |
| `about` | ○ グループ化 | category.svg |
| `textviewer` | ○ グループ化 | file-text.svg |
| `canvas` | ✕ 個別表示 | app-window.svg |

- グループ化された複数ウィンドウ表示時のボタン形式:
  - グループボタン例: `[Terminal] ×2` (タイプ名 + 個数)
  - グループをホバーまたはクリックするとドロップダウンで個別ウィンドウ一覧を表示
  - ドロップダウン内の各項目をクリックすると対応ウィンドウをアクティブ化して前面表示
- Canvas ウィンドウは個別表示（グループ化なし）

**ボタン操作**:
- ボタンクリック:
  - グループボタン: ドロップダウンを表示/非表示
  - 個別ボタン: 該当ウィンドウをアクティブ化して前面表示
  - 既にアクティブなウィンドウのボタン再クリック: ウィンドウを最小化（非表示）※将来機能
- ボタンホバー: グループボタンの場合、ドロップダウンを表示

**更新タイミング**:
- ウィンドウ作成時: 対応するボタンを追加
- ウィンドウ削除時: 対応するボタンを削除
- ウィンドウタイトル変更時: ボタンテキストを更新
- ウィンドウアクティブ状態変化時: ボタンのスタイルを更新
- 若干の遅延は許容（イベント駆動または定期更新）

### 5.7 メッセージ表示言語

- **ダイアログ表示**：日本語を使用
  - ファイル取り込みサイズ超過時: 「ファイルの取り込みに失敗しました: 合計サイズ (X.XX MiB + Y.YY MiB) が 1.5 MiB の制限を超えています」
  - ファイル実行不可: 「このファイルは実行できません: filename」
- **ターミナル出力（成功メッセージ）**：英語を使用
  - ファイル取り込み成功: 「Imported filename (XXX KB)」

## 6. ウィンドウ仕様

### 6.1 構造

```
+-----------------------------------+
| <アイコン> <タイトル> (パディング) <閉じるボタン> |
+-----------------------------------+
| <ウィンドウの内容>                   |
+-----------------------------------+
```

- 枠線: 1px solid black
- タイトルバー: HTML 要素で描画
- タイトルバー左端: ウィンドウタイプに応じたアイコン（16x16px）
  - アイコン色：タイトルバー文字色と同一
  - アクティブ時: 白色 (#fff)
  - 非アクティブ時: 黒色 (#111)
- タイトルバー右端: クローズボタン
  - アイコン: `src/assets/icons/x.svg`（16x16px）
  - アイコン色：タイトルバー文字色と同一

### 6.2 ウィンドウ種別と初期仕様

各ウィンドウタイプの基本仕様は以下の通り。詳細は 6.5 を参照。

| タイプ | アプリID | タイトル | アイコン | 生成方法 | インスタンス |
|--------|---------|---------|---------|---------|--------|
| `terminal` | `f9c271dc-49ee-4295-811a-dc2bf7bd27a7` | Terminal | terminal-2.svg | 新規端末ボタン | 複数可 |
| `canvas` | `8faf5f5e-66ee-4580-8a8f-fe64ed78ea23` | タスク定義 | app-window.svg | Worker 起動 | 複数可 |
| `filemanager` | `c1a107bb-20ec-4d3d-ad94-944d5bce863d` | File System | folder.svg | メニュー/複数ウィンドウ | 1個限定 |
| `about` | `88a37a04-c8e5-42e6-9f13-2f7fd31f62c3` | About | category.svg | メニュー | 1個限定 |
| `textviewer` | `e97b8139-cebf-40cd-8805-a0b87192c50f` | ファイル名 | file-text.svg | ファイル実行 | 複数可 |
| `onboarding` | `d1b3e8f4-7a2e-4c9d-b5f1-9a8c6d2e4f3b` | ようこそ | app-window.svg | 初期化時自動生成 | 1個限定 |

### 6.3 識別子

#### 6.3.1 ウィンドウID

- 各ウィンドウ生成時にユニーク ID を採番
- ID をキーとして以下を実施
  - 前面化
  - 移動
  - クローズ

#### 6.3.2 アプリID

- 各アプリケーションタイプに固有の UUIDv4 を割り当て、アプリケーション種別を識別
- ウィンドウタイプと一対一対応
- 用途：
  - ウィンドウタイプの分類・管理
  - ローカルストレージのキー体系（将来拡張時）
  - ウィンドウルールの適用判定（例: 1 個限定ウィンドウかどうか）

**各アプリのアプリID:**
- Terminal: `f9c271dc-49ee-4295-811a-dc2bf7bd27a7`
- Rust Task (Canvas): `8faf5f5e-66ee-4580-8a8f-fe64ed78ea23`
- File Manager: `c1a107bb-20ec-4d3d-ad94-944d5bce863d`
- About: `88a37a04-c8e5-42e6-9f13-2f7fd31f62c3`
- Text Viewer: `e97b8139-cebf-40cd-8805-a0b87192c50f`
- Onboarding: `d1b3e8f4-7a2e-4c9d-b5f1-9a8c6d2e4f3b`

注記:
- UI で扱う `windowId` は UUIDv4 文字列を採用する。
- Rust 側では Wasm 境界の簡潔性のため数値ハンドルを使用し、Worker 内で `Rust数値ID <-> UI UUID` を変換する。

### 6.4 ウィンドウの挙動

#### 6.4.1 新規作成

- 新規端末ボタン押下時、端末ウィンドウを追加
- 端末生成直後、自動的に `VER` コマンドを内部実行してアプリバージョンを表示
  - 入力行（`>` プロンプト）は表示されない
  - VER の実行結果のみが画面に表示される
- VER 実行後、入力テキストボックスへフォーカス

#### 6.4.2 前面化とアクティブ化

- ウィンドウ内任意領域タップ時、他ウィンドウより背面であれば最前面へ移動
- ウィンドウがクリックされた場合、そのウィンドウがアクティブになる
- 入力テキストボックスや実行ボタンなど操作要素のクリックでは、フォーカス阻害を防ぐため前面化起因の再描画を行わない
- **コマンド入力欄のタップ挙動** (ターミナルウィンドウ):
  - ターミナルが非アクティブなときにコマンド入力欄をタップした場合、ウィンドウをアクティブにしてコマンド入力欄にフォーカスを移動する

#### 6.4.3 キーボード入力とアクティブ化

- ウィンドウ内のサブアプリケーションのキーボード操作は、そのウィンドウがアクティブな場合のみ受け付ける
- ターミナルウィンドウ: コマンド入力がアクティブな場合のみ動作
- ファイルマネージャウィンドウ: 矢印キー（上下）、Enter キーがアクティブな場合のみ動作
- **ビジュアル表現** (アクティブウィンドウ):
  - タイトルバー背景色: 青色 (#0b5ed7)
  - タイトルバーテキスト色: 白 (#fff)
  - 主ボタン（実行ボタン、OKボタンなど）: 青地 (#0b5ed7) に白文字
- **ビジュアル表現** (非アクティブウィンドウ):
  - タイトルバー背景色: 灰色 (#c0c0c0)
  - タイトルバーテキスト色: 黒 (#111)
  - 主ボタン: 灰色地 (#c0c0c0) に黒文字

#### 6.4.4 移動

- タイトルバーをドラッグすると位置変更
- ドラッグ中にポインタがデスクトップ外へ出た場合、ポインタ座標をデスクトップ境界でクリップし、それ以上外側へ移動しない
- 上方向へ移動する際、ウィンドウの `y` は 0 未満にならない

#### 6.4.5 閉じる

- クローズボタン押下で対象ウィンドウのみ削除
- クローズボタン表示形式:
  - Vite の `?raw` インポートで SVG (`src/assets/icons/x.svg`) をインライン展開

### 6.5 ウィンドウ種別別仕様

#### 6.5.1 端末ウィンドウ

**基本情報:**
- タイプ: `terminal`
- タイトル: `'Terminal'`
- アイコン: `src/assets/icons/terminal-2.svg`
- 生成方法: タスクバーの新規端末ボタン
- インスタンス: 複数可

**レイアウト:**
```
+-----------------------------------+
| <タイトル> (パディング) <閉じるボタン> |
+-----------------------------------+
| <ターミナル画面>                 |
+-----------------------------------+
| <コマンド入力テキスト>    <実行ボタン> |
+-----------------------------------+
```

**仕様:**
- ターミナル画面は白地ダークグレー文字
- 文字が下端を超えたら自動スクロール
- **スクロールバッファ管理:**
  - スクロールバッファの最大行数: 1000 行（定数 `TERMINAL_MAX_LINES` で管理）
  - 上限を超えた場合は古い行から自動削除
  - テキストに改行文字（`\n`）が含まれる場合は別行として挿入
- **コマンド履歴機能:**
  - ターミナルウィンドウごとに独立したコマンド履歴を管理
  - 履歴は永続化されず、セッション中のみ保持
  - **上キー（ArrowUp）**：古いコマンドを履歴から呼び出す
  - **下キー（ArrowDown）**：新しいコマンドを履歴から呼び出す、最後は入力中のコマンドに戻る
  - **入力途中のテキスト保存**：履歴ブラウズ開始時に現在の入力を保存し、ブラウズ後に復元可能
  - **重複排除**：同じコマンドを再実行した場合、古い履歴を削除して新しいものを先頭に追加
  - **初期化コマンド除外**：ターミナルウィンドウ生成時に自動実行される `VER` コマンドは履歴に残らない
- ウィンドウ生成直後、初期化メッセージを表示せず、自動的に `VER` コマンドを実行
- ユーザが入力したコマンドの実行により出力が画面に表示される
- 定義済みコマンド：VER, ECHO, CLS, DIR, TYPE, COPY, DEL, REN, SET, HELP, START, NCST
- 未定義コマンドはファイル実行として処理
- ウィンドウをクローズすると、そのウィンドウの履歴はクリアされる

#### 6.5.2 Rust タスクウィンドウ（Canvas）

**基本情報:**
- タイプ: `canvas`
- タイトル: タスク定義
- アイコン: `src/assets/icons/app-window.svg` (デフォルト)
- 生成方法: 端末コマンド経由で Worker + Rust(Wasm) により生成
- インスタンス: 複数可

**仕様:**
- コンテンツ領域は Canvas 要素
- ウィンドウサイズとタイトルは Rust 側で指定
- Canvas への描画は Rust 側が `js_draw_image` インターフェースで実施

**キーボード入力ハンドリング:**
- Canvas ウィンドウがアクティブ（フォーカス）な時、すべてのキーボード入力イベント（keydown/keyup）を対応する Worker に転送
- Main スレッドで `document.addEventListener('keydown/keyup', ...)` で捕捉
- アクティブウィンドウが Canvas 型かつ `workerByWindowId` に登録されている場合、`keyboardEvent` メッセージを Worker に `postMessage`
- Worker は受け取ったキーボード情報をコンソール出力でログ（デバッグ用）
- 将来：Rust 側の FFI 関数 `js_on_keyboard_event` 経由で Rust コードへ転送予定
- Canvas ウィンドウ以外がアクティブの場合、キーボード入力は転送されない

#### 6.5.3 ファイルマネージャウィンドウ

**基本情報:**
- タイプ: `filemanager`
- タイトル: `'File System'`
- アイコン: `src/assets/icons/folder.svg`
- 生成方法: ハンバーガーメニューの「ファイル」から起動
- インスタンス: 1個限定（既に起動済みの場合は最前面化）

**初期位置と位置記憶:**
- **初期位置**: デスクトップ右上（右端から20px、上端から40px）
- **位置記憶**: ウィンドウをドラッグで移動させた場合、クローズ時にその位置をメモリに保持
  - メモリ内でのみ保持（localStorage には保存しない）
  - ページリロード時にはリセットされ、右上の初期位置に戻る
  - 再度開くと、前回のセッション内での最後の位置に復元される

**ファイル一覧表示:**
- ファイルシステムのすべてのファイルを一覧表示
- ファイル名の先頭にアイコンを表示（拡張子に基づいて異なるアイコン）

**ファイル選択:**
- カーソルキー (上下矢印) またはマウスクリックでファイルを選択
- 選択されたファイルはハイライト表示
- **ウィンドウフォーカス切替時に選択状態を保持する**
- **選択項目の背景色**:
  - ウィンドウがアクティブな場合: 青 (#0b5ed7) に白文字
  - ウィンドウが非アクティブな場合: グレー (#c0c0c0) に黒文字

**スクロール位置保持:**
- ファイルマネージャーのスクロール位置をメモリに保持
- ウィンドウ間でフォーカスを切り替えてもスクロール位置は復元される
- ウィンドウをクローズするとスクロール位置情報はクリーンアップされる

**ファイル実行/オープン:**
- 選択ファイルで Enter キー押下またはダブルクリック時、以下の処理を実行:
  - 拡張子が環境変数 `PATH_EXT` に含まれる場合:
    - Rust Task を起動（`start <filename>`）
  - 拡張子が `.bmp` または `.jpg` の場合（大文字小文字不区別）:
    - `gview` ファイルをファイルシステムから検索
      - 直接 `gview` という名前で検索
      - 見つからない場合、`gview` + PATH_EXT の各拡張子（例：`gview.hrb`）で検索
    - 見つかった場合、Rust Task のコマンド `gview <filename>` を実行
    - 見つからない場合、エラーダイアログ「このファイルは実行できません: gview」を表示
  - 拡張子が `.txt` の場合:
    - `tview` ファイルをファイルシステムから検索
      - 直接 `tview` という名前で検索
      - 見つからない場合、`tview` + PATH_EXT の各拡張子（例：`tview.hrb`）で検索
    - 見つかった場合、Rust Task のコマンド `tview -w80 -h24 <filename>` を実行
    - 見つからない場合、エラーダイアログ「このファイルは実行できません: tview」を表示
  - 拡張子が `.mml` の場合（大文字小文字不区別）:
    - `mmlplay` ファイルをファイルシステムから検索
      - 直接 `mmlplay` という名前で検索
      - 見つからない場合、`mmlplay` + PATH_EXT の各拡張子（例：`mmlplay.hrb`）で検索
    - 見つかった場合、Rust Task のコマンド `mmlplay <filename>` を実行
    - 見つからない場合、エラーダイアログ「このファイルは実行できません: mmlplay」を表示
  - 上記いずれにも該当しない場合:
    - エラーダイアログ「このファイルは実行できません: filename」を表示

**ファイルシステム監視:**
- 他のウィンドウでのファイル作成・削除・リネームを監視
- 変更時に一覧を自動更新

**[TBD] ファイル操作:**
- ファイルの削除・リネーム機能（インターフェース仕様 TBD）

##### 6.5.3.1 ファイルアイコン仕様

- アイコン出典: [Tabler Icons](https://tabler-icons.io) (MITライセンス)
- アイコンファイル: `src/assets/icons/` に配置
  - `app-window.svg`
  - `file-text.svg`
  - `file.svg`
  - `photo.svg`
  - `file-music.svg`
- アイコン表示ルール:
  - **実行ファイル** (拡張子が `PATH_EXT` に含まれる場合、大文字小文字不区別):
    - アイコン: `src/assets/icons/app-window.svg`
  - **テキストファイル** (拡張子が `.txt` の場合、大文字小文字不区別):
    - アイコン: `src/assets/icons/file-text.svg` (文書風のフラットアイコン)
  - **画像ファイル** (拡張子が `.bmp` または `.jpg` の場合、大文字小文字不区別):
    - アイコン: `src/assets/icons/photo.svg` (写真アイコン)
  - **音楽ファイル** (拡張子が `.mml` の場合、大文字小文字不区別):
    - アイコン: `src/assets/icons/file-music.svg` (音楽ファイルアイコン)
  - **その他のファイル**:
    - アイコン: `src/assets/icons/file.svg` (白地のフラットファイルアイコン)
- アイコン表示形式:
  - Vite の `?raw` インポートで SVG をインライン展開
  - サイズ: 16x16px (CSS で `width: 16px; height: 16px` を指定)
  - 色: `currentColor` (CSS の `color: #333` を継承)

**画像ファイル実行動作:**
- `.bmp` または `.jpg` 拡張子（大文字小文字不区別）のファイルを実行時:
  - `gview` ファイルをファイルシステムから検索（ターミナルコマンドと同じ PATH_EXT 検索ロジック）
  - 見つかった場合、Rust Task のコマンド `gview <filename>` を実行し、画像ビューアアプリケーションが起動される
  - 見つからない場合、エラーダイアログを表示

**音楽ファイル実行動作:**
- `.mml` 拡張子（大文字小文字不区別）のファイルを実行時:
  - `mmlplay` ファイルをファイルシステムから検索（ターミナルコマンドと同じ PATH_EXT 検索ロジック）
  - 見つかった場合、Rust Task のコマンド `mmlplay <filename>` を実行し、音楽再生アプリケーションが起動される
  - 見つからない場合、エラーダイアログを表示

#### 6.5.4 About ウィンドウ

**基本情報:**
- タイプ: `about`
- タイトル: `'About'`
- アイコン: `src/assets/icons/category.svg`
- 生成方法: ハンバーガーメニューの「About...」から起動
- インスタンス: 1個限定（既に起動済みの場合は最前面化）

**レイアウト:**
```
+-----------------------------------+
| About...        <閉じるボタン> |
+-----------------------------------+
| <アプリ名（大きく表示）>            |
| <バージョン> | <Gitハッシュ>        |
| +-------------------------------+ |
| | <ライセンス文（スクロール可）>    | |
| +-------------------------------+ |
|                         <OKボタン> |
+-----------------------------------+
```

**アプリ情報:**
- アプリ名: `HariboteBox`
  - フォントサイズ: 16px（太字）
  - 独立した行に表示
- バージョン情報: 別行に表示
  - フォーマット: `v{__APP_VERSION__} | {__GIT_HASH__}`
  - フォントサイズ: 11px（グレー色）

**ライセンス文:**
- MIT ライセンステキスト
- Markdownマークアップ対応（`**text**` で太字表示）
- テキスト表示領域に HTML レンダリング表示
- 最大高さ: 200px
- オーバーフロー時はスクロール可能

**ボタン:**
- OK ボタン押下でウィンドウをクローズ
- 既に About ウィンドウが表示されている場合は新規作成しない（最前面化のみ）

#### 6.5.5 テキストビューアウィンドウ

**基本情報:**
- タイプ: `textviewer`
- タイトル: 対象ファイル名
- アイコン: `src/assets/icons/file-text.svg`
- 生成方法: ファイルマネージャから `.txt` ファイルを実行時に生成
- インスタンス: 複数可（同一ファイルでも複数開くことが可能）

**レイアウト:**
```
+-----------------------------------+
| <ファイル名>         <閉じるボタン> |
+-----------------------------------+
| <テキスト内容（読み取り専用）>     |
| （スクロール可能）                |
+-----------------------------------+
```

**テキストボックス:**
- 読み取り専用（`readonly` 属性）
- 複数行テキスト
- 白地黒文字
- テキスト量に応じてスクロール可能
- フォント: monospace
- クリック時にウィンドウをアクティブ化

**スクロール位置保持:**
- テキストビューアのスクロール位置をメモリに保持
- ウィンドウ間でフォーカスを切り替えてもスクロール位置は復元される
- ウィンドウをクローズするとスクロール位置情報はクリーンアップされる

**ウィンドウ仕様:**
- デフォルトサイズ: 幅 600px × 高さ 400px
- 初期配置: ウィンドウサイズから計算してデスクトップ中央に配置
- 複数ウィンドウ時: カスケード配置

**ファイル内容:**
- UTF-8 デコード
- バイナリファイルの場合は UTF-8 デコード失敗時エラーダイアログを表示

**インスタンス管理:**
- 同一ファイルを複数回開く場合、毎回新規ウィンドウを作成
- 既にビューアで開かれている別ファイルのウィンドウ情報は保持

#### 6.5.6 ようこそウィンドウ (Onboarding)

**基本情報:**
- タイプ: `onboarding`
- タイトル: `'ようこそ'`
- アイコン: `src/assets/icons/app-window.svg`
- 生成方法: アプリケーション初期化時に自動生成
- インスタンス: 1個限定（既に起動済みの場合は最前面化）

**ウィンドウ仕様:**
- デフォルトサイズ: 幅 640px × 高さ 546px（背景画像 514px + タイトルバー 32px）
- 初期配置: ウィンドウサイズから計算してデスクトップ中央に配置
- 背景画像: `src/assets/bliss.png`（640 × 514px、デスクトップ風景）

**レイアウト:**
```
+-----------------------------------+
| <タイトル>      <閉じるボタン>     |
+-----------------------------------+
| <背景画像: bliss.png>          |
| <コンテンツ（テキスト、ボタン）>   |
+-----------------------------------+
```

**コンテンツ:**
1. **ウェルカムメッセージ**
   - テキスト: 「ようこそ！HariboteBox」

2. **説明テキスト**
   - 「はりぼてOS」というOSのアプリケーションをエミュレーターで実行する環境です。ウィンドウシステムとシェルを通じて、OS上のプログラムを操作できます。

3. **機能紹介リスト**
   - ターミナル: コマンド入力・実行
   - ファイル管理: ファイルシステムの表示・操作
   - ウィンドウ操作: 移動、前面化、クローズ
   - ファイル取り込み: ドラッグ&ドロップ対応

4. **ヒントセクション**（背景色: 薄青 `#f0f7ff`、左ボーダー: 青 `#0b5ed7`）
   - ウィンドウ操作のコツ
   - HELP コマンドで使用可能なコマンドを確認
   - デスクトップへのドラッグ&ドロップでファイルを取り込み

5. **「開始する」ボタン**
   - クリックでウィンドウをクローズ
   - 位置: コンテンツの下部（`margin-top: auto` で下部に固定）

**スタイル:**
- コンテンツエリアは背景画像の上に重ねられる
- テキストは白地背景なしで直接表示（背景画像が見えるように配置）
- ボタンは主色（青 #0b5ed7）を使用

**注記：コンテンツの具体的な内容は本設計書のスコープ外**
- ウェルカムメッセージ、説明テキスト、機能リスト、ヒント、ボタンテキストなどの具体的な文言は、README 更新と同様に人間がメンテナンスする
- 本設計書では UI 構造・レイアウト・スタイルのみを定義
- コンテンツ更新は `src/main.ts` の `createOnboardingPanel()` 関数内で直接編集

### 6.6 ファイルシステム仕様

#### 6.6.1 基本仕様

- ドライブ、ディレクトリは持たないフラット構造
- 各ファイルは以下を保持
  - ファイル名 (表示名)
  - ファイル内容 (`Uint8Array`)
  - `isInitialFile` フラグ (初期ファイルかどうか)
- ファイルアクセスは大文字小文字を区別しない
  - `ABC == abc == Abc`
  - 内部では canonical key (ファイル名の大文字化) で一意管理
  - 同一 canonical key の作成/リネーム/コピーは上書き扱い
- ファイル名の表示は元の大文字小文字を保持

#### 6.6.2 ファイル名正規化ルール

- 入力値はパスの末尾要素 (Last Path Component) を対象とする
- 使用可能文字
  - ASCII 英数字 (`A-Z`, `a-z`, `0-9`)
  - ASCII 記号: `.` (ドット)、`_` (アンダースコア)、`!` (感嘆符)
  - 上記以外の ASCII 記号は `_` に変換
- 文字コード 127 超の文字が多数を占める場合は変換エラー
  - 現実装の判定: 非 ASCII 文字数が全体の過半数
- 連続する `_` は 1 つに圧縮
- 先頭/末尾の `.` は除去
- 長すぎる場合は最大 31 文字に切り詰め
  - 拡張子は可能な限り優先して保持
  - 拡張子が長すぎる場合はベース名を最低 8 文字残す

#### 6.6.3 初期ファイルと永続化

**初期ファイルフラグ:**
- 各ファイルは内部的に `isInitialFile` フラグを保持
  - `true`: ビルド時に生成した初期ファイル群に由来するファイル
  - `false`: ドラッグアンドドロップで取り込んだ、または実行時に新規生成したファイル

**localStorage への保存ルール:**
- 永続化対象: `isInitialFile === false` のファイルのみ
  - 初期ファイルは localStorage に保存しない
  - ドラッグアンドドロップで取り込んだファイルのみ保存
  - 実行時に新規生成したファイルのみ保存
- ファイル作成・更新・削除・リネーム操作時に、対象ファイルが初期ファイルでない場合のみ localStorage に保存
  - キー: `haribote.fs.v1`
  - 保存内容: zlib による圧縮とBase64 エンコード
  - **圧縮形式:**
    - テキスト形式：`{ version: 1, files: [...] }` (JSON)
    - 圧縮：zlib で圧縮
    - エンコード：圧縮済みバイナリを base64 文字列に変換
    - localStorage に保存される値は base64 文字列のみ
  - **復元時の処理:**
    - base64 デコード → zlib 展開 → JSON パース
    - 復元後のファイル配列には `isInitialFile === false` のファイルのみ含まれる

**初期ファイル上書き処理:**
- 初期ファイルと同じ名前 (canonical key 一致) のファイルをドラッグアンドドロップまたは実行時生成した場合
  - 既存の初期ファイルを上書き
  - 上書き後、`isInitialFile` フラグを `false` に変更
  - その時点で新規ファイルとして localStorage に保存
  - 次回起動時に localStorage から復元され、初期ファイルを上書きした状態で起動

**初期ファイル削除の動作:**
- セッション内（起動中）に初期ファイルを削除した場合
  - メモリ上からファイルが削除される
  - 削除情報は localStorage に保存されない（初期ファイルは localStorage に保存されていないため）
  - ブラウザを再読み込みすると初期ファイルが復活する

**起動時の初期化処理:**
1. ビルド時に生成した初期ファイル群をメモリへ読み込む
   - すべてのファイルに `isInitialFile = true` フラグを設定
   - 初期ファイルの元データは `src/initial-fs` 配下に置く
   - ビルド時に生成スクリプトが `src/generated-initial-fs.ts` を作成し、アプリ側で読み込む
   - **圧縮処理:**
     - ビルド時：各ファイルを zlib で圧縮してから base64 エンコード（`scripts/generate-initial-fs.mjs` で処理）
     - 起動時：base64 デコード後に zlib で展開（`applyInitialFiles()` で処理）
     - 圧縮により初期ファイルシステムのバンドルサイズを削減
     - 起動時の展開処理は `pako` ライブラリで実施（既存の localStorage 圧縮と同一）
2. localStorage から永続化データを読み込み、初期ファイルシステムにマージする
   - localStorage キー `haribote.fs.v1` に保存データがある場合のみ実行
   - zlib 展開 → base64 デコード → JSON パース
   - マージ時の処理:
     - 保存されたファイル配列の各ファイルについて
     - 同じ名前 (canonical key 一致) の初期ファイルが存在する場合、初期ファイルを上書き
     - 初期ファイルに存在しないファイルは新規追加
     - マージ後のすべてのファイルについて、`isInitialFile` フラグを保持（保存データ側は `false`）

**デスクトップへの drag&drop:**
- ファイル取り込み時のサイズチェック
  - 既存ファイルのバイナリサイズ合計と取り込みデータのバイナリサイズ合計が `1.5MiB` (`1,572,864` bytes) を超える場合、エラーダイアログで表示して取り込みを中止
  - エラーメッセージはダイアログで日本語で表示
  - 既存ファイルシステムは変更しない
- 取り込み時
  - `File.webkitRelativePath` または `File.name` の末尾要素をファイル名に使用
  - 同名 (canonical key 一致) は上書き
  - 取り込まれたファイルは `isInitialFile = false` に設定
  - localStorage に保存される

#### 6.6.4 ファイルシステム管理方針（複数 Worker 対応）

- **Main での一元管理**：
  - メイン UI スレッドでファイルシステム全体を管理
  - すべての作成・更新・削除操作を Main 経由で実施
  - localStorage への永続化も Main のみが行う
  - デスクトップへの drag&drop（ファイル取り込み）処理も Main で実施
    - サイズチェックが必要な場合は Main で判定し、エラー時は取り込みを中止
  
- **Worker でのアクセス**：
  - 起動時に Main から最新スナップショットを受け取る
  - ファイル読み込みはスナップショット内で実施
  - ファイル書き込みは Main へメッセージで通知
  - Main からの `updateFileSystemSnapshot` を受信したら内部状態を最新化
  
- **複数 Worker の同期**：
  - 任意の Worker が書き込みを実施
  - Main が永続化
  - Main が全 Worker に更新スナップショットを配信
  - 次のアクセスで全 Worker が最新ファイルシステムを参照可能

### 6.7 端末コマンド仕様

- コマンド入力テキストボックスで Enter 押下、または実行ボタン押下でコマンドを実行
- 実行後に入力テキストボックスをクリア

#### 6.7.1 コマンド掲載ポリシー

- 定義済みコマンドの総数が多いと、ユーザの認知負荷が増加する
- そのため、**HELPコマンドには選定されたコマンドのみを掲載する**
- すべてのコマンドをリストアップするのではなく、基本的かつ頻用されるコマンドに限定
- このポリシーにより、ユーザは必要最小限の情報で基本操作を学習できる

#### 6.7.2 定義済みコマンド

- **定義済みコマンド:**
  - `VER`: アプリバージョンと git ハッシュを表示
  - `ECHO`: 入力パラメータをそのまま表示
  - `CLS`: 現在端末の表示をクリア
  - `DIR`: ファイル一覧とサイズ、合計サイズを表示
  - `LS`: DIR のエイリアス
  - `TYPE <filename>`: テキストとしてファイル内容を表示
  - `COPY <source> <destination>`: ファイルを複製
  - `DEL <filename>`: ファイルを削除
  - `REN <source> <destination>`: ファイル名変更
  - `EXIT`: 実行した端末ウィンドウを閉じる
  - `SET <name>=<value>`: 環境変数を設定 (例: `SET PATH_EXT=.hrb:.exe`)
    - 環境変数名に `=` が含まれない場合のみ、既存の環境変数値を表示
    - 例: `SET PATH_EXT` で現在の `PATH_EXT` 値を表示
  - `HELP`: 基本的なコマンドの一覧と簡単な説明を表示（掲載コマンド: VER, DIR, TYPE, NCST）
  - `START <filename> [args...]`: ダミーターミナルでタスク実行（出力非表示）
    - Rust タスクを起動するが、出力をターミナルに表示しない
    - 起動エラーは `console.error` にのみ出力される
    - パラメータは通常のタスク起動と同じ
    - 例: `START foo bar` → `foo.hrb` を起動、コマンドラインは `foo bar` として渡される
    - デフォルトターミナルへのフォールバック出力も行われない
  - `NCST <filename> [args...]`: ダミーターミナルでタスク実行（出力非表示）
    - START コマンドと完全に同じ動作
    - 例: `NCST foo bar` → `foo.hrb` を起動、コマンドラインは `foo bar` として渡される
  - `OPEN <filename> [args...]`: START / NCST のエイリアス（ダミーターミナルでタスク実行、出力非表示）
    - START と NCST と完全に同じ動作

#### 6.7.3 未定義コマンド

- 上記定義コマンドに一致しない場合:
  - 先頭トークンをファイル名として検索
  - 検索手順:
    1. 正規化したコマンド名がファイルシステム上に存在するか確認
    2. 存在しない場合、`PATH_EXT` 環境変数にある拡張子を先頭トークンに追加して検索
    3. `PATH_EXT` は `:` で区切られた拡張子リスト (例: `.hrb:.exe:.bin`)
    4. いずれかのパターンでファイルが見つかればそれを使用
  - ファイルが見つかった場合、Worker を起動 (起動引数は `terminalWindowId`・`fileName`・`commandLine`)
  - ファイルもコマンドも見つからない場合は「Bad command or file name」を表示

## 7. 状態管理設計

### 7.1 状態モデル

```ts
type WindowId = string;  // UUIDv4 形式のウィンドウ固有ID

type AppId = string;  // UUIDv4 形式のアプリケーション種別ID

type WindowKind = 'canvas' | 'terminal' | 'filemanager' | 'about' | 'textviewer';

type WindowModel = {
  id: WindowId;  // ウィンドウ個別ID
  appId: AppId;  // ウィンドウタイプのアプリID
  kind: WindowKind;
  title: string;
  x: number;
  y: number;
  width: number;
  height: number;
  zIndex: number;
  isActive: boolean;
};

type EnvironmentVariables = {
  [key: string]: string;
};

type WindowGroupInfo = {
  appId: AppId;
  kind: WindowKind;
  windowIds: WindowId[];  // グループに属するウィンドウID リスト
  isExpanded: boolean;    // グループのドロップダウン展開状態
};

type AppState = {
  windows: WindowModel[];
  nextZIndex: number;
  env: EnvironmentVariables;
  isMenuOpen: boolean;
  defaultTerminalWindowId: WindowId | null;
  activeWindowId: WindowId | null;
  windowGroups: WindowGroupInfo[];  // ウィンドウグループ化情報（タスクバー表示用）
};

// アプリID定数
const APP_IDS = {
  TERMINAL: 'f9c271dc-49ee-4295-811a-dc2bf7bd27a7',
  CANVAS: '8faf5f5e-66ee-4580-8a8f-fe64ed78ea23',
  FILE_MANAGER: 'c1a107bb-20ec-4d3d-ad94-944d5bce863d',
  ABOUT: '88a37a04-c8e5-42e6-9f13-2f7fd31f62c3',
  TEXT_VIEWER: 'e97b8139-cebf-40cd-8805-a0b87192c50f',
} as const;
```

### 7.1.1 実装定数

| 定数名 | 値 | 説明 |
|--------|-----|------|
| `TASKBAR_HEIGHT_PX` | 48 | タスクバーの高さ |
| `TERMINAL_WINDOW_OFFSET_STEP` | 24 | 新規端末ウィンドウのカスケード配置オフセット |
| `TERMINAL_MAX_LINES` | 1000 | ターミナルのスクロールバッファ最大行数 |
| `TITLE_BAR_HEIGHT` | 32 | ウィンドウのタイトルバーの高さ |
| `Z_INDEX_REFRESH_THRESHOLD` | 10,000 | Z インデックス最適化の閾値 |
| `INITIAL_TERMINAL_WINDOW_HEIGHT` | 320 | 初期端末ウィンドウの高さ |
| `INITIAL_TERMINAL_WINDOW_WIDTH` | 520 | 初期端末ウィンドウの幅 |
| `INITIAL_TERMINAL_X` | 40 | 最初の端末ウィンドウの X 座標 |
| `INITIAL_TERMINAL_Y` | 40 | 最初の端末ウィンドウの Y 座標 |

### 7.1.2 環境変数初期値

- `PATH_EXT`: `.hrb` (拡張子リスト、`:` で区切る)

### 7.1.3 メニュー状態初期値

- `isMenuOpen`: `false` (起動時はメニュー非表示)
- `defaultTerminalWindowId`: `null` (起動時は端末がないため null)
- `activeWindowId`: `null` (起動時はアクティブなウィンドウなし)
- `windowGroups`: `[]` (起動時はグループなし)

### 7.1.4 ターミナル履歴管理

実装でのグローバル状態（AppState に含まれない付帯情報）:

- `terminalHistoryByWindow: Map<WindowId, string[]>`
  - 各ターミナルウィンドウの過去実行コマンド履歴
  - 最新のコマンドが配列の先頭（インデックス 0）
  - 永続化されない（セッション中のみ保持）
  - ウィンドウクローズ時にクリア

- `terminalHistoryIndexByWindow: Map<WindowId, number>`
  - 各ターミナルウィンドウの現在の履歴ブラウズ位置
  - `-1`: 履歴ブラウズ中ではない（入力中の状態）
  - `0以上`: 履歴配列のインデックス
  - ウィンドウクローズ時にクリア

- `terminalPendingInputByWindow: Map<WindowId, string>`
  - 履歴ブラウズ開始時に入力途中のテキストを一時保存
  - 履歴ブラウズ中に下キーで最後の位置に戻った場合に復元
  - コマンド実行時にクリア
  - ウィンドウクローズ時にクリア

**履歴管理ルール:**
- **コマンド実行時の処理**:
  1. `skipHistory` オプションが指定されていない場合のみ履歴に追加
  2. 重複チェック：同じコマンドが履歴に存在する場合、古い方を削除
  3. 先頭に追加：正規化されたコマンドを履歴配列の先頭に `unshift`
  4. ブラウズ位置リセット：`historyIndex = -1`
  5. 保留中入力クリア：`pendingInput` を削除
  6. **初期化コマンド除外**：ターミナルウィンドウ生成時の自動 `VER` 実行では `skipHistory: true` を指定

- **上キー（ArrowUp）での操作**:
  1. 初回ブラウズ時：現在の入力を `pendingInput` に保存
  2. `historyIndex` をインクリメント（古い履歴へ移動）
  3. 最大値（`history.length - 1`）でクランプ
  4. 入力フィールドに履歴内容を表示

- **下キー（ArrowDown）での操作**:
  1. `historyIndex` をデクリメント（新しい履歴へ移動）
  2. `historyIndex === -1` になった場合は `pendingInput` を復元
  3. 負の値にならないよう処理
  4. 入力フィールドを更新

### 7.2 状態更新ルール

- ウィンドウ追加:
  - 新規 ID 発行
  - 1 個限定ウィンドウの判定:
    - `appId` が `APP_IDS.FILE_MANAGER` または `APP_IDS.ABOUT` の場合、同じ `appId` を持つウィンドウが既に存在するかチェック
    - 存在する場合はそのウィンドウを最前面化のみ（新規作成しない）
    - 存在しない場合のみ新規作成
  - 端末ウィンドウの初期座標:
    - **カスケード配置**で左上(40, 40)から開始
    - 新規ウィンドウ作成時に内部カウンタを増やし、毎回オフセット（24px）を加算
    - `x = 40 + (nextTerminalSpawnIndex * 24)`、`y = 40 + (nextTerminalSpawnIndex * 24)`
    - デスクトップ外へはみ出す場合は `clampWindowToDesktop()` でデスクトップ内へ収納
  - Rust/Canvas ウィンドウの初期座標:
    - ウィンドウサイズから計算してデスクトップ中央に配置
  - `zIndex = nextZIndex` で追加後、`nextZIndex` をインクリメント
  - 端末ウィンドウ追加時:
    - `defaultTerminalWindowId` が null の場合のみ、新規追加されたウィンドウ ID を `defaultTerminalWindowId` に設定
    - `defaultTerminalWindowId` が既に設定されている場合は変更しない
- 前面化:
  - 対象の `zIndex` が最大未満の場合のみ、`zIndex = nextZIndex` に更新
  - 更新後に `nextZIndex` をインクリメント
- 移動:
  - ドラッグ差分で `x,y` 更新
  - 通常時は `x,y` をクランプしない (はみ出し許可)
  - ドラッグ中の差分計算に使用するポインタ座標のみデスクトップ境界でクランプ
  - `y` は最小値 0 でクランプする
- 個別削除:
  - `id` 一致要素を削除
  - 削除対象が `defaultTerminalWindowId` に一致する場合:
    - 削除後、残存する端末ウィンドウが存在すればそのうち最初のものを新しい `defaultTerminalWindowId` に設定
    - 残存する端末ウィンドウがない場合は `defaultTerminalWindowId` を null に設定
  - 削除対象が `activeWindowId` に一致する場合:
    - 削除後、他のウィンドウが存在すればそのうち最も上前にあるもの（最大 zIndex）を新しい `activeWindowId` に設定
    - 他にウィンドウがない場合は `activeWindowId` を null に設定
- アクティブウィンドウの切り替え:
  - ウィンドウをクリックすると `activeWindowId = windowId` に設定
  - `bringToFrontIfNeeded()` を呼び出した時に自動的にそのウィンドウをアクティブ化
  - ウィンドウ内のサブアプリケーションがアクティブウィンドウのみキー入力を受け付ける
- 全削除:
  - `windows = []`
  - `nextZIndex = 1` にリセット
- 環境変数設定:
  - `env[name] = value` で更新
- メニューの表示/非表示:
  - `isMenuOpen = true` でメニュー表示
  - `isMenuOpen = false` でメニュー非表示
- メニュー項目の実行:
  - ターミナル選択時: 新規端末ウィンドウを作成、メニュー自動クローズ
  - ファイル選択時: ファイルマネージャウィンドウを起動（既存インスタンスがあれば最前面化）、メニュー自動クローズ
  - About... 選択時: About ウィンドウを起動（既存インスタンスがあれば最前面化）、メニュー自動クローズ

## 8. イベント設計

### 8.1 UI イベント

- クリック (ハンバーガーメニューボタン): メニュー表示/非表示トグル
- クリック (メニュー外部): メニューをクローズ
- ESC キー: メニューをクローズ
- クリック (新規端末メニュー項目): 新規端末作成、メニュー自動クローズ
- クリック (ファイルメニュー項目): ファイルマネージャウィンドウを起動、メニュー自動クローズ
- タイマー更新: 時計表示の更新
- ポインタダウン: ウィンドウ前面化判定
- ポインタダウン (タイトルバー): ドラッグ開始
- ポインタムーブ: ドラッグ中位置更新
- ポインタアップ/キャンセル: ドラッグ終了
- クリック (閉じる): 個別削除
- Enter または実行ボタン: 端末コマンド実行
- **上下キー (ターミナルウィンドウ)**: コマンド履歴ブラウズ
  - 上キー（ArrowUp）: 古いコマンドを履歴から呼び出す
  - 下キー（ArrowDown）: 新しいコマンドを履歴から呼び出す、または入力中のテキストに戻す
- dragover: 既定動作を抑止
- drop: ファイル取り込み（サイズチェック: 既存+新規合計が 1.5MiB 超過時はエラーダイアログを表示して中止）
- ファイルマネージャでのカーソルキー (上下): ファイル選択移動
- ファイルマネージャでのマウスクリック: ファイル選択
- ファイルマネージャでの Enter キー/ダブルクリック: ファイル実行（拡張子に応じた動作）
- ファイルシステム変更: ファイルマネージャ自動更新

### 8.2 イベント処理順序

- タイトルバー操作時:
  - 前面化
  - ドラッグ開始
- 本文操作時:
  - 前面化のみ

### 8.3 アプリケーション初期化

**起動時に自動生成されるウィンドウ:**

1. **ファイルシステム初期化**
   - localStorage から保存ファイルを復元（存在しない場合は初期ファイルを読み込み）

2. **デフォルト端末ウィンドウ**
   - 最初の端末ウィンドウを自動作成
   - 自動的に `VER` コマンドを実行
   - デフォルト端末として設定

3. **ファイルマネージャウィンドウ**
   - ファイルシステム初期化後に自動作成
   - 初期位置: デスクトップ右上（右端から20px、上端からターミナルと同じ高さ）
   - デフォルトサイズ: 幅 300px × 高さ 400px
   - アクティブ状態で表示

4. **ようこそ（Onboarding）ウィンドウ**
   - デスクトップ中央に自動生成
   - ユーザーが「開始する」ボタンをクリックするまで表示

## 9. 画面コンポーネント案

- AppRoot
- DesktopArea
- TaskbarPanel
- HamburgerMenuButton
- MenuPopup
- MenuItem (新規端末)
- MenuItem (ファイル)
- ClockDisplay
- WindowLayer
- WindowFrame
- TitleBar
- FileIcon (拡張子に基づくアイコン表示)

## 10. アクティブウィンドウシステム

### 10.1 概要

複数ウィンドウが存在する場合、キーボード入力を受け付けるウィンドウを明確にするための仕組み。アクティブウィンドウのみがサブアプリケーション（ファイルマネージャ、ターミナルなど）のキー操作を受け付ける。

### 10.2 ビジュアル表現

- **アクティブウィンドウ**:
  - タイトルバー背景色: `#0b5ed7` (青)
  - タイトルバーテキスト色: 白 (#fff)
  - タイトルバーアイコン色: 白 (#fff)
  - フォント太さ: Bold
  - ファイルマネージャのリストボーダー: 2px solid #0b5ed7
  - ターミナルのボーダーセパレーター: 青色
  
- **非アクティブウィンドウ**:
  - タイトルバー背景色: `#c0c0c0` (灰色)
  - タイトルバーテキスト色: 黒 (#111)
  - タイトルバーアイコン色: 黒 (#111)
  - フォント太さ: Normal
  - リスト/セパレーターボーダー: 灰色

### 10.3 キー操作の仕様

- **ファイルマネージャ**:
  - アクティブな場合のみ以下のキー入力を受け付ける
  - `ArrowUp`: ファイルリストで選択を上に移動
  - `ArrowDown`: ファイルリストで選択を下に移動
  - `Enter`: 選択ファイルを実行/オープン
  
- **ターミナル**:
  - アクティブな場合のみ入力テキストボックスでのキー入力が有効
  - 非アクティブ時のキー入力は無視

### 10.4 アクティブ化のトリガー

- **初期アクティブ化**: 起動後、最初のウィンドウ作成時に自動的にアクティブ化
- **クリックによるアクティブ化**: ウィンドウ内の任意領域をクリックするとそのウィンドウがアクティブ化
- **前面化時のアクティブ化**: `bringToFrontIfNeeded()` 呼び出し時に自動的にアクティブ化
- **リスト内のクリック**: ファイルマネージャのファイルリストをクリックするとそのウィンドウがアクティブ化

### 10.5 非機能要件

- マウス/タッチの双方で操作できる実装を推奨
- 画面リサイズ時にも破綻しないレイアウト
- 単一ページ遷移なしで操作継続

## 11. 仕様確認リスト

以下は実装時の確認項目。各項目が仕様を満たしていることを検証。

- 各ウィンドウが UUIDv4 形式の固有 windowId を持つ
- 各ウィンドウがアプリID（appId）をウィンドウモデルに保持する
- Terminal ウィンドウの appId が `f9c271dc-49ee-4295-811a-dc2bf7bd27a7` である
- Canvas ウィンドウの appId が `8faf5f5e-66ee-4580-8a8f-fe64ed78ea23` である
- File Manager ウィンドウの appId が `c1a107bb-20ec-4d3d-ad94-944d5bce863d` である
- About ウィンドウの appId が `88a37a04-c8e5-42e6-9f13-2f7fd31f62c3` である
- Text Viewer ウィンドウの appId が `e97b8139-cebf-40cd-8805-a0b87192c50f` である
- 起動時に青緑デスクトップと下部タスクバーが表示される
- タスクバー左端にハンバーガーメニューボタンが表示される
- ハンバーガーメニューボタン押下でメニューが表示される
- メニュー表示中に再度ボタン押下でメニューがクローズされる
- メニュー外部をクリックするとメニューがクローズされる
- ESC キー押下でメニューがクローズされる
- メニュー内の「ターミナル」をクリックすると端末ウィンドウが新規作成される
- 新規端末作成後、メニューが自動的にクローズされる
- 新規端末ウィンドウは左上(40, 40)の位置に表示される
- 複数の端末ウィンドウを作成する際、毎回24pxずつ右下にカスケード配置される
- ファイルマネージャウィンドウの初期位置はデスクトップ右上（右端から20px、上端から40px）である
- ファイルマネージャウィンドウを移動してからクローズした場合、メモリ内にその位置が記憶される
- ファイルマネージャウィンドウを再度開いた場合、前回のセッション内での最後の位置に復元される
- ページリロード時には、ファイルマネージャウィンドウの位置記憶がリセットされ、右上初期位置に戻る
- ウィンドウがデスクトップ外へはみ出す場合、デスクトップ内へ収納される
- タスクバーの時計が現在時刻を表示し、定期更新される
- メニューポップアップはウィンドウより前面に配置される
- 端末ウィンドウ生成直後、入力テキストボックスがフォーカスされる
- 入力テキストボックスと実行ボタンがマウスクリックで操作できる
- 端末画面は白地黒文字で、出力増加時に自動スクロールする
- Enter または実行ボタンでコマンド実行後、入力テキストボックスがクリアされる
- `VER` 実行でアプリバージョンと git ハッシュが表示される
- `ECHO` 実行で入力パラメータがそのまま表示される
- `DIR/TYPE/COPY/DEL/REN/CLS` が仕様どおり動作する
- `HELP` コマンド実行で選定されたコマンド（VER, DIR, TYPE, NCST）の説明が表示される
- `EXIT` コマンド実行時に実行した端末ウィンドウが閉じられる
- `SET <name>=<value>` で環境変数が設定される
- `SET <name>` で環境変数の現在値が表示される
- `DIR` コマンド実行時にファイル一覧が表示される
- `LS` コマンド実行時にファイル一覧が表示される（`DIR` のエイリアス）
- `START <filename> [args...]` および `NCST <filename> [args...]` および `OPEN <filename> [args...]` コマンド実行時、ダミーターミナルでタスクが起動される
  - タスク出力がターミナルに表示されない
  - デフォルトターミナルへのフォールバック出力も発生しない
  - 起動エラーは `console.error` にのみ出力される
  - コマンドライン引数として「START」「NCST」「OPEN」を含まず、ファイル名以降のみが渡される
  - `OPEN` は `START` / `NCST` のエイリアス
- 定義済み以外のコマンド入力時、先頭トークンが既存ファイル名なら Worker が起動される
- ファイルもコマンドも見つからない場合、「Bad command or file name」が表示される
- 上記 Worker 起動時に `terminalWindowId`・`fileName`・`commandLine` が引数として渡される
- Worker の起動は `startWithCommand` のみで、起動引数が必須である
- Worker が `println` / `print` インターフェースで指定端末へデバッグ出力できる
- デスクトップへの drag&drop でファイルが取り込まれる
- 取り込み時に既存ファイルと取り込みデータのバイナリサイズ合計が `1.5MiB` (`1,572,864` bytes) を超える場合、エラーダイアログで表示して取り込みを中止する
- 取り込み失敗時にエラーメッセージがダイアログで表示される
- 取り込み失敗時に既存ファイルシステムが変更されない
- localStorage に保存データが存在する場合は起動時に復元し、存在しない場合はビルド時初期ファイル群を読み込む
- ファイルアクセスが大文字小文字非依存である (`ABC == abc == Abc`)
- ファイル名表示は入力時の大文字小文字を保持する
- 端末コマンド経由で Rust(Wasm) が Worker 上で実行される
- Rust 側が指定したウィンドウサイズとタイトルでウィンドウが生成される
- UI の `windowId` は UUIDv4 である
- Rust 側で扱う windowId は数値ハンドルで、Worker が UI UUID へ変換する
- `open_window` の戻り値として Rust 側へ返るのは数値ハンドルである
- Rust 側から戻り値の数値ハンドル指定でウィンドウ操作インターフェースを呼べる
- Rust 側から Canvas imageData 一括描画インターフェースを呼べる
- 各ウィンドウがユニーク ID を持つ
- 背面ウィンドウをタップすると最前面になる
- タイトルバーのドラッグでウィンドウ位置が変わる
- 閉じるボタンで対象ウィンドウが削除される
- Rust タスクウィンドウの内容領域が Canvas である
- 環境変数 `PATH_EXT` の初期値は `.hrb` である
- 未定義コマンド実行時、先頭トークンに `PATH_EXT` の拡張子を追加したファイルが検索される
- `PATH_EXT` は `:` で区切られた拡張子リスト形式である
- 起動時にはメニューが表示されていない (`isMenuOpen = false`)
- Main がファイルシステムを一元管理する
- 複数 Worker が同時実行時、各 Worker は Main から最新スナップショットを受け取る
- Worker が `js_write_file` で書き込みを実施すると、Main が全 Worker へ `updateFileSystemSnapshot` メッセージを配信する
- Worker が スナップショット更新メッセージを受け取ると、内部の fileSystem が最新化される
- 複数 Worker 実行時、一方の Worker の書き込み内容が、他方の Worker で読み込める
- ファイル書き込み後の `persistFileSystem()` は Main でのみ実行される
- localStorage への永続化は原子的に実行される（二重書き込みなし）
- メニュー内に「ターミナル」と「ファイル」の 2 つ項目が表示される
- メニュー内の「ファイル」をクリックするとファイルマネージャウィンドウが起動される
- メニュー内に「About...」が表示される
- ファイルマネージャウィンドウは同一フォルダの別ウィンドウを起動しない（最前面化）
- ファイルマネージャはファイルシステムのすべてのファイルを一覧表示する（ファイル名の先頭にアイコンを表示）
- ファイルマネージャで最初の端末がデフォルト端末に設定される
- 最初の端末を閉じると、次の端末がデフォルト端末に昇格する
- すべての端末が閉じると、デフォルト端末は null 状態（エラーにならず無言）
- 端末が 1 個のみ存在する場合、その端末がデフォルト端末として使用される
- ファイルマネージャでファイルをカーソルキーで選択できる
- ファイルマネージャでファイルをマウスクリックで選択できる
- ファイルマネージャでウィンドウフォーカスを切り替えても選択状態を維持する
- ファイルマネージャのアクティブウィンドウ内の選択項目背景色は青 (#0b5ed7) に白文字
- ファイルマネージャの非アクティブウィンドウ内の選択項目背景色はグレー (#c0c0c0) に黒文字
- ファイルマネージャのスクロール位置はウィンドウフォーカス切替時に保持される
- ファイルマネージャで Enter キーまたはダブルクリックでファイル実行
- `PATH_EXT` に含まれる拡張子のファイルをファイルマネージャで実行すると Worker が起動される
- `.txt` ファイルをファイルマネージャで実行すると、`tview` ファイルが PATH_EXT で検索されて `tview -w80 -h24 <filename>` コマンドが実行される
- `.bmp` または `.jpg` ファイルをファイルマネージャで実行すると、`gview` ファイルが PATH_EXT で検索されて `gview <filename>` コマンドが実行される
- `.mml` ファイルをファイルマネージャで実行すると、AudioContext を resume した後、`mmlplay` ファイルが PATH_EXT で検索されて `mmlplay <filename>` コマンドが実行される
- 画像ファイル（`.bmp`, `.jpg`）のアイコンは `photo.svg` で表示される
- 音楽ファイル（`.mml`）のアイコンは `file-music.svg` で表示される
- 実行不可ファイルをファイルマネージャで実行するとエラーダイアログが表示される
- ファイルマネージャは他のウィンドウでのファイル操作を監視して自動更新する
- デフォルト端末への不明なメッセージ出力は受け付けない（エラーにならない）
- 複数 Worker 同時実行時、ファイルマネージャに最新のファイル一覧が反映される
- About... 選択時に About ウィンドウが表示される
- About ウィンドウに「About」タイトルが表示される
- About ウィンドウにアプリ名、バージョン、Git ハッシュが別行で表示される
- About ウィンドウにライセンス文が HTML レンダリングで表示される（Markdown太字形式 `**text**` 対応）
- About ウィンドウのライセンス文がオーバーフロー時はスクロール可能
- About ウィンドウの OK ボタン押下でウィンドウがクローズされる
- About ウィンドウが既に表示されている場合、再度開こうとすると最前面化される
- ファイルマネージャでテキストファイル（`.txt`）を実行すると、`tview` ファイルが PATH_EXT で検索されて `tview -w80 -h24 <filename>` コマンドが実行される
- テキストビューアウィンドウはファイル名をタイトルとして表示する
- テキストビューアのテキストボックスは読み取り専用である
- テキストビューアのテキストボックスは複数行表示可能でスクロール可能である
- **ターミナルコマンド履歴機能:**
  - ターミナルウィンドウごとに独立したコマンド履歴が保持される
  - 上キー（ArrowUp）で古いコマンドを呼び出せる
  - 下キー（ArrowDown）で新しいコマンドを呼び出せる、または入力中のテキストに戻せる
  - 入力途中のコマンドがある状態で履歴を呼び出すと、入力テキストが一時保存される
  - 履歴ブラウズ中に下キーで最後の位置に戻ると、保存された入力テキストが復元される
  - コマンドを実行すると一時保存したテキストはクリアされる
  - 他の履歴コマンドを実行すると保存テキストはクリアされる
  - ウィンドウクローズ時に履歴と保存テキストがクリアされる
  - 永続化されない（セッション中のみ保持）
  - 同じコマンドを複数回実行した場合、古い履歴が削除され最新のみが保持される（重複排除）
  - 複数ターミナルが開いている場合、各ターミナルの履歴は独立している
  - ターミナルウィンドウ生成時の初期化コマンド（`VER`）は履歴に残らない
- テキストビューアのデフォルトサイズは 600px × 400px である
- テキストビューアは複数ウィンドウ開始時にカスケード配置される
- テキストビューアウィンドウは同一ファイルでも複数開くことが可能である
- テキストビューアの内容は UTF-8 でデコードされる
- テキストビューアで UTF-8 デコード失敗時はエラーダイアログが表示される
- テキストビューアウィンドウのタイプは `textviewer` である
- テキストビューアのテキストボックスをクリックするとウィンドウがアクティブ化される
- テキストビューアのスクロール位置はウィンドウフォーカス切替時に保持される
- ウィンドウタイトルバーの左端にアイコンが表示される
- ターミナルウィンドウのタイトルバーアイコンは `terminal-2` である
- テキストビューアのタイトルバーアイコンは `file-text` である
- ファイルマネージャのタイトルバーアイコンは `folder` である
- About ウィンドウのタイトルバーアイコンは `category` である
- Rust タスクウィンドウのタイトルバーアイコンは `app-window` である
- アクティブウィンドウのタイトルバーアイコン色は白 (#fff) である
- 非アクティブウィンドウのタイトルバーアイコン色は黒 (#111) である
- 各ファイルは内部的に `isInitialFile` フラグを保持する
- 初期ファイルは localStorage に保存されない
- ドラッグアンドドロップで取り込んだファイルのみ localStorage に保存される
- 実行時に新規生成したファイルのみ localStorage に保存される
- 起動時に初期ファイル群でファイルシステムを初期化する
- 起動時に localStorage から保存データを読み込み、初期ファイルシステムにマージする
- localStorage のマージ時に同名ファイルが存在する場合、localStorage のデータで上書きされる
- 初期ファイルと同じ名前のファイルをドラッグアンドドロップで取り込んだ場合、`isInitialFile` フラグが `false` に変更される
- 初期ファイルと同じ名前のファイルを実行時生成した場合、`isInitialFile` フラグが `false` に変更される
- 初期ファイルと同じ名前で上書きされたファイルは次回起動時に localStorage から復元される
- 初期ファイルをセッション内で削除した場合、その情報は localStorage に保存されない
- ブラウザ再読み込み後、削除した初期ファイルが復活する
- localStorage の保存データは `isInitialFile === false` のファイルのみ含める
- 複数 Worker 実行時、ファイルシステム更新メッセージに `isInitialFile` フラグが含まれる

**タスクバーウィンドウ一覧機能**:
- タスクバーにウィンドウボタングループ領域が配置される（ハンバーガーメニューと時計の間）
- ウィンドウ作成時にタスクバーにボタンが追加される
- ウィンドウ削除時にタスクバーのボタンが削除される
- 同じタイプのウィンドウが複数ある場合、1 つのグループボタンで表示される
- グループボタン表示形式は `[タイプ名] ×個数` である
- Canvas ウィンドウは個別表示される（グループ化されない）
- グループボタンをクリックするとドロップダウンメニューが表示される
- ドロップダウンメニュー内の個別ウィンドウをクリックするとそのウィンドウが前面化される
- ドロップダウン外をクリックするとすべてのドロップダウンが閉じられる
- ウィンドウタイトル変更時にボタンテキストが更新される
- アクティブウィンドウのボタンは背景が青色 (#0b5ed7)、文字が白 (#fff) で表示される
- 非アクティブウィンドウのボタンはデフォルトスタイルで表示される
- ボタンホバー時に背景が薄い灰色 (#e9ecef) で表示される
- グループボタン内のウィンドウがアクティブな場合、グループボタン自体もハイライト表示される
- `windowGroups` は ウィンドウ操作（作成・削除）時に再計算される
- `WindowGroupInfo` は `appId`、`kind`、`windowIds`、`isExpanded` を保持する

**Onboarding ウィンドウ:**
- Onboarding ウィンドウの appId が `d1b3e8f4-7a2e-4c9d-b5f1-9a8c6d2e4f3b` である
- アプリケーション初期化時に Onboarding ウィンドウが自動生成される
- Onboarding ウィンドウのサイズが幅 640px × 高さ 546px である（背景 514px + タイトルバー 32px）
- Onboarding ウィンドウが画面中央に配置される
- 背景画像 `src/assets/bliss.png` が表示される
- Onboarding ウィンドウに「ようこそ！HariboteBox」というタイトルが表示される
- 機能説明リスト（ウィンドウ操作、ファイル管理、ターミナル、タスクバーなど）が表示される
- 「ヒント」セクションが薄青背景 (#f0f7ff) で左ボーダー（青 #0b5ed7）とともに表示される
- ヒント内容に「ウィンドウ操作のコツ」「HELPコマンド」「ドラッグ&ドロップ」が含まれる
- 「開始する」ボタンがウィンドウ下部に配置されている（`margin-top: auto` で固定）
- 「開始する」ボタンをクリックすると Onboarding ウィンドウがクローズされる
- Onboarding ウィンドウがクローズ時に状態がクリーンアップされる

## 12. 詳細仕様確認

本セクションは、セクション 11 の仕様確認リストを補完する、より詳細な実装確認項目を記載。

- ウィンドウを移動する際、ウィンドウの位置情報がメモリに保持される
- ウィンドウが最前面化されるときに、z-index が最大値に更新される
- ウィンドウがクローズされるとき、該当ウィンドウを参照していた状態がクリーンアップされる
- ファイルマネージャ内のファイルリストは、ファイルシステムの変更を検知して自動更新される
- ファイルマネージャでのキー入力が他のウィンドウに伝播しない
- 端末ウィンドウでの複数行入力を適切に処理する（改行コード処理）
- 端末出力がバッファサイズを超えた場合のスクロール処理
- Worker 実行中のメッセージ処理が非ブロッキングで実行される

## 13. アーキテクチャ設計補足

本セクションは、セクション 1-8 で述べたアーキテクチャの実装上の詳細を補足。

### 14.1 Main スレッドの状態管理

- AppState は単一インスタンスで、全ウィンドウの状態を一元管理
- ウィンドウの作成・削除・移動などの操作は、AppState を経由して行われる
- 状態変更時は renderWindows() が自動的に呼び出され、画面が更新される

### 14.2 Worker との通信プロトコル

- Message passing による非同期通信
- Rust コード内で js_* 関数呼び出し → Worker が main へメッセージ送信
- main が対応する状態変更処理を実行 → Worker へ応答メッセージ返送

### 14.3 localStorage による永続化

- 起動時に localStorage から初期ファイルシステムを復元
- ファイルシステムの変更は自動的に localStorage へ保存
- 複数 Worker が同時に実行される場合、最後の変更が優先される

## 14. テスト基準

### 15.1 アクティブウィンドウシステムのテスト基準

- ウィンドウをクリックするとそのウィンドウがアクティブ化される
- アクティブウィンドウのタイトルバーが青色 (#0b5ed7) で表示される
- 非アクティブウィンドウのタイトルバーが灰色で表示される
- アクティブなファイルマネージャのリストボーダーが2px青色で表示される
- 非アクティブなファイルマネージャのリストボーダーが灰色で表示される
- ファイルマネージャがアクティブな場合のみ矢印キー（上下）が動作する
- ファイルマネージャがアクティブな場合のみ Enter キーが動作する
- ファイルマネージャが非アクティブな場合、矢印キーを押しても無反応
- ファイルマネージャをクリックするとアクティブになり、フォーカスが移動する
- 他のウィンドウがアクティブな場合、ファイルマネージャのキー操作は受け付けない
- 複数ウィンドウ表示時に、各ウィンドウが個別にアクティブ化・非アクティブ化できる

## 15. 未実装・検討中項目

### 16.1 確認待ち事項（人間の指示待ち）

1. 非 ASCII 多数判定の閾値を「過半数」から変更する必要があるか
2. `TYPE` 実行時のバイナリファイル表示方法 (現状は UTF-8 デコード)

### 16.2 [TBD] ファイル操作機能

- ファイルマネージャでのファイル削除・リネーム機能（インターフェース仕様 TBD）

### 16.3 [TBD] テキストビューア詳細表示

- ファイルマネージャからテキストファイル実行時の詳細挙動仕様（詳細 TBD）

## 16. 変更管理ルール

- 一定以上の変更を加える前に、本設計書を先に更新する。
- README は AI では更新しない。
- 不足情報がある場合は実装前に人間へ確認する。
- localStorage の利用キーとデータ構造は本設計書に明記し、変更時は差分を追記する。

## 17. Rust/Wasm インターフェース設計

### 18.0 Rust タスク実装方針

**現在の実装状況:**
- Rust タスク (`run_task`) は主にテスト・デバッグ用のメッセージログとダミー Canvas 描画が中心
- Window API、Canvas 描画、ファイル I/O のインターフェースは動作確認済み
- println/print による端末への出力が正常に機能

**将来の拡張予定:**
- より複雑なアプリケーション実装（画像処理、音声処理、ゲーム等）
- インタラクティブな入力処理（キーボード・マウスイベント）
- Canvas への高頻度描画（アニメーション・ゲームループ）
- Worker 間通信や複数タスクの同時実行
- 外部 Rust クレート（image, audio 等）の統合

現在のインターフェース設計（セクション 14.1 - 14.4）は、これらの将来拡張を想定した設計となっており、既存インターフェースの互換性を保ちつつ拡張可能な構造になっています。

### 18.1 Rust エントリポイント関数

#### `run_task(file_name, command_line, title_bar_height)`
- wasm_bindgen でエクスポートされたメイン実行関数
- Worker から起動される正式なエントリポイント
- パラメータ:
  - `file_name: String`: 実行対象ファイルの名前
  - `command_line: String`: 完全なコマンドライン文字列
  - `title_bar_height: u32`: ウィンドウタイトルバーの高さ（ピクセル）
- Worker の起動時に自動的に呼び出される
- `title_bar_height` は UI の `TITLE_BAR_HEIGHT` 定数値（通常 32px）で、Rust アプリケーションがウィンドウレイアウト計算時に参照可能

### 18.2 Rust から利用するインターフェース

Rust 側は以下の JavaScript 関数を `#[wasm_bindgen(module = "env")]` 経由で呼び出します：

実装の詳細:
- `src/wasm/env.ts` が `env` モジュールとして提供される
- Worker (`rustTask.worker.ts`) が `createWasmEnv()` で実装関数を作成し、`globalThis.wasmEnv` へ登録
- `env.ts` の各関数が `globalThis.wasmEnv` を参照して実装へ委譲

**インターフェース更新時の注意:**
- Rust 側で新しいインターフェース関数を追加する場合（`lib.rs` の `#[wasm_bindgen(module = "env")]` に追加）、以下を同時に更新必須：
  - `rustTask.worker.ts` の `WasmEnv` 型定義に関数シグネチャを追加
  - `rustTask.worker.ts` の `createWasmEnv()` 内に実装を追加
  - `src/wasm/env.ts` にエクスポート関数を追加（委譲）
  - 本設計書（14.2）にドキュメント記載

- `js_open_window(width, height, title_ptr, title_len) -> u32`
  - Rust 側が指定したサイズ・タイトルでウィンドウを新規作成
  - Worker が Rust 数値ハンドルを採番し、戻り値として Rust 側へ返却
  - Worker 内で Rust 数値ハンドルと UI UUID を関連付ける
- `js_move_window(window_id, x, y)`
  - 指定ハンドルに対応するウィンドウの位置を変更
- `js_activate_window(window_id)`
  - 指定ハンドルに対応するウィンドウをアクティブ化
- `js_close_window(window_id)`
  - 指定ハンドルに対応するウィンドウを閉じる
- `js_draw_image(window_id, x, y, width, height, ptr, len)`
  - 指定ハンドルに対応するウィンドウの Canvas の指定矩形領域に RGBA imageData を描画
  - パラメータ:
    - `x`, `y`: 描画開始位置 (Canvas 内の座標)
    - `width`, `height`: 描画サイズ (ピクセル)
- `js_print(text_ptr, text_len)`
  - Rust から Worker の `js_print` を呼び、Main の端末出力へテキストを追記する (改行なし)
  - 出力先端末は Worker 起動引数の `terminalWindowId` を使用する
- `js_read_file_size(filename_ptr, filename_len) -> i32`
  - 指定ファイル名のファイルサイズをバイトで返す
  - ファイルが存在しない場合は負値を返す
  - ファイル名照合は大文字小文字を区別しない
- `js_read_file_into(buf_ptr, buf_len) -> i32`
  - 前回の `js_read_file_size` で取得したファイル内容をバッファへ読み込む
  - 読み込んだバイト数を返す
- `js_write_file(filename_ptr, filename_len, data_ptr, data_len, mode) -> i32`
  - 指定ファイル名でファイル内容を書き込む
  - `mode` は以下の値：
    - `0`: update (存在する場合は上書き、存在しない場合はエラー)
    - `1`: create (存在しない場合は新規作成、存在する場合はエラー)
    - `2`: upsert (存在する場合は上書き、存在しない場合は新規作成)
  - 成功時は 0 を返す
- `js_get_keyboard_event(window_id) -> i32`
  - Worker のグローバルイベントキューから次のキーボードイベントをデキュー
  - 戻り値: イベントコード（文字キーの場合は ASCIIコード、キューが空の場合は -1）
  - `window_id` パラメータは受け付けるが、すべてのウィンドウが同一グローバルキューを共有するため、キュー特定には使用されない
  - 詳細は 18.7 を参照
- `js_get_tick() -> f64`
  - Worker 初期化からの経過時間をミリ秒単位で返す
  - Worker 起動時に `performance.now()` を記録し、現在の `performance.now()` との差分を計算
  - 戻り値: 経過時間（ミリ秒、小数値）
  - 用途: Rust タスク内でタイマー・フレームスキップ・アニメーション制御等に使用
- `js_schedule_event(delay_ms, event_code)`
  - 指定時間後にイベントコードをイベントキューにエンキュー
  - `setTimeout` を使用して遅延実行を実現
  - パラメータ:
    - `delay_ms`: 遅延時間（ミリ秒）
    - `event_code`: エンキューするイベントコード（通常は ASCIIコードまたは特殊キーコード）
  - 用途: Rust タスク内でスケジュール化されたイベント（タイマーイベント等）を生成

### 18.3 Worker と Main のメッセージインターフェース

#### Main → Worker (Main が Worker へ送信するメッセージ)

- `startWithCommand`
  - Worker 起動時に Main から送信されるメッセージ
  - 起動引数：`terminalWindowId`、`fileName`、`commandLine`、`titleBarHeight`、`fileSystemSnapshot`、`environmentVariables`
  - `titleBarHeight`: UI の `TITLE_BAR_HEIGHT` 定数値（通常 32px）
  - 詳細は 14.1 参照

- `keyboardEvent(eventType, key, code, keyCode, ctrlKey, shiftKey, altKey, metaKey, isAutoRepeat)`
  - Canvas ウィンドウがアクティブ時、キーボード入力イベントを Worker に転送
  - Main スレッドで Canvas ウィンドウの keydown/keyup イベントを捕捉し、アクティブウィンドウ判定後に転送
  - パラメータ:
    - `eventType`: 'keydown' | 'keyup' | 'keypress'
    - `key`: キーの文字表現 (e.g., 'a', 'Enter', 'ArrowUp')
    - `code`: キーボード位置コード (e.g., 'KeyA', 'Enter', 'ArrowUp')
    - `keyCode`: 数値キーコード（廃止予定だが互換性のため含める）
    - `ctrlKey`, `shiftKey`, `altKey`, `metaKey`: 修飾キー状態（boolean）
    - `isAutoRepeat`: キーホールド時の自動リピート状態（boolean）
  - Worker は受け取ったイベント情報をログ出力（将来：Rust FFI 経由で Rust タスクに転送予定）
  - Canvas ウィンドウ以外がアクティブの場合、転送されない

- `updateFileSystemSnapshot(fileSystemSnapshot)`
  - 複数 Worker 同時実行時、ファイルシステムが変更された際に Main から配信
  - Worker が `js_write_file` でファイルを書き込む
    ↓
  - Main がファイルシステムを更新して localStorage へ永続化
    ↓
  - Main が全アクティブ Worker に本メッセージを配信
  - Worker はスナップショットを受け取り、内部の fileSystem を更新
  - その後の `js_read_file_*` 呼び出しで、他の Worker の書き込み内容が読める

- `windowClose(windowId)`
  - Canvas ウィンドウのクローズボタン押下時、Main から Worker に通知
  - パラメータ:
    - `windowId`: クローズするウィンドウの UUID
  - Worker の処理フロー:
    1. `windowIdMap` から対応する数値ハンドルを逆検索
    2. 数値ハンドルと UUID のマッピングを削除（クリーンアップ）
    3. `windowIdMap.size === 0` （すべてのウィンドウが閉じられた）の判定
    4. 全ウィンドウ閉じられた場合、100ミリ秒のタイマーをセット
    5. タイマー経過後、再度 `windowIdMap.size === 0` を確認
    6. 依然としてウィンドウが存在しない場合、Worker は終了（`done` メッセージを Main に送信）
    7. タイマー待機中に新しいウィンドウが開かれた場合は、Worker は継続実行
  - 用途: Rust タスク内でウィンドウクローズイベントを検出し、適切にリソースをクリーンアップして終了する

#### Worker → Main (Worker が Main へ送信するメッセージ)

- `done`
  - Worker がタスク完了または終了する際に Main へ通知
  - パラメータなし
  - 送信契機:
    1. Rust タスクの main ルーチンが正常に終了（全ウィンドウが閉じられた後、2秒タイマーで確認）
    2. Rust タスクが処理フロー完了
  - Main の処理:
    - Worker スレッドを終了
    - Worker インスタンスをクリーンアップ
    - 関連する UI（ターミナルウィンドウ等）を適切に状態更新

- `error(message, stack)`
  - Worker でエラーが発生した際に Main へ通知
  - パラメータ:
    - `message`: エラーメッセージ文字列
    - `stack`: スタックトレース（オプション）
  - 発生契機:
    - WASM モジュール読み込み失敗
    - WASM ランタイム初期化失敗
    - Rust タスク実行エラー
    - 予期しない例外発生
  - Main の処理:
    - エラーをターミナルに出力
    - Worker を終了
    - ユーザーへのエラー通知

- `fileWritten(filename)`
  - Worker が `js_write_file` でファイルを書き込んだ際、Main へ通知
  - パラメータ:
    - `filename`: 書き込みされたファイル名
  - Main の処理:
    - ファイルシステムを更新して localStorage へ永続化
    - 複数 Worker 同時実行時は、全 Worker に `updateFileSystemSnapshot` メッセージで配信

- `println(windowId, text)`
  - Worker から Main へ改行付きテキスト出力要求を送る
  - Main は指定 `windowId` の端末画面へ `text` を 1 行追加する
  - `windowId` が端末として存在しない場合は無視する
- `print(windowId, text)`
  - Worker から Main へ改行なしテキスト出力要求を送る
  - Main は指定 `windowId` の端末画面の現在行末へ `text` を追記する
  - `windowId` が端末として存在しない場合は無視する
- `drawImage(windowId, x, y, width, height, pixels)`
  - Worker から Main へ Canvas への画像描画要求を送る
  - Worker が Rust の `js_draw_image` を通じて受け取った RGBA データを Canvas に描画
  - パラメータ:
    - `windowId`: Canvas ウィンドウの UUID
    - `x`, `y`: 描画開始位置 (Canvas 内の座標)
    - `width`: 画像幅 (ピクセル)
    - `height`: 画像高さ (ピクセル)
    - `pixels`: RGBA データ (Uint8Array、各ピクセルが RGBA で 4 バイト)
  - Main は指定ウィンドウの Canvas に `putImageData` で指定座標へ描画
  - `windowId` が Canvas ウィンドウとして存在しない場合は無視する

### 18.4 実行フロー

#### 単一 Worker の場合

1. 端末でコマンドライン入力
2. 未定義コマンドかつファイル名一致時、Worker を `startWithCommand` で起動
3. Worker 生成、Wasm モジュールをロード
4. wasm_bindgen でエクスポートされた `run_task(terminalWindowId, fileName, commandLine, titleBarHeight)` を呼び出す
5. Rust はインターフェースを通じてメインスレッドへ操作要求を送信
6. メインスレッドがウィンドウ状態更新および Canvas 描画を実施
7. Rust 関数が終了 (panic または return)
8. Worker は Main へ `done` または `error` メッセージを送信

#### 複数 Worker 同時実行時のファイルシステム同期

- **初期化時**：
  1. Worker A 起動 → Main から最新スナップショット (v0) を受け取る
  2. Worker B 起動 → Main から最新スナップショット (v0) を受け取る

- **ファイル書き込み時**：
  1. Worker A が `js_write_file("file.txt", data)` を呼び出す
  2. Main がファイル書き込みメッセージを受け取る
  3. Main が fileSystem を更新
  4. Main が `persistFileSystem()` で localStorage へ永続化
  5. Main が全アクティブ Worker（A, B）に `updateFileSystemSnapshot(v1)` メッセージを配信
  6. Worker B が スナップショット更新メッセージを受け取る
  7. Worker B が内部の fileSystem を v1 へ更新
  8. Worker B が次に `js_read_file_size("file.txt")` を呼び出す
  9. Worker B は Worker A の書き込み内容を正常に読み込める ✓

### 18.5 未定義コマンド実行フロー

1. 端末でコマンドライン入力
2. 定義済みコマンドに一致しない場合、先頭トークンをファイル名として探索
3. ファイルが存在する場合、Worker を `startWithCommand` で起動
4. Worker は起動引数を受け取り、`println(windowId, text)` でデバッグ出力
5. Rust も `println(text)` / `print(text)` を呼べる
6. Worker は通常の Rust(Wasm) タスク実行を継続

**複数 Worker が並行実行する場合**：
- 各 Worker は Main から受け取ったスナップショットをベースに動作
- Task A が `js_write_file` で新規ファイルを作成
- Main が永続化 → 全 Worker に `updateFileSystemSnapshot` を配信
- Task B が その後 `js_read_file_size` でファイルを検索 → 正常に見つかる
- ファイルの重複上書きは、Main の `fileSystem` Map で最後の write が優先

### 18.6 Canvas ウィンドウのキーボード入力フロー

#### ユーザーが Canvas ウィンドウでキーを押した場合

1. **Main スレッド - キー入力イベント捕捉**：
   - `document.addEventListener('keydown', (event) => {...})`
   - すべてのキーボード keydown イベントを捕捉
   - `state.activeWindowId` でアクティブウィンドウ ID を取得

2. **Main スレッド - ウィンドウ型判定**：
   - `findWindowById(state.activeWindowId)` でウィンドウモデルを検索
   - ウィンドウの `kind` が `'canvas'` であるか確認

3. **Main スレッド - Worker 検索**：
   - `workerByWindowId.get(state.activeWindowId)` で対応する Worker を検索
   - Canvas ウィンドウが `js_open_window` FFI で生成された時に自動登録される
   - Worker が見つからない場合は処理を中断

4. **Main スレッド - キーボードイベント転送**：
   - 見つかった Worker に `keyboardEvent` メッセージを `postMessage` で送信
   - メッセージ形式:
     ```typescript
     {
       type: 'keyboardEvent',
       windowId: 'uuid-string',  // Canvas ウィンドウの UUID
       eventType: 'keydown',  // or 'keyup'
       key: 'a',              // キーの文字表現
       code: 'KeyA',          // キーボード位置コード
       keyCode: 65,           // 数値コード（互換性用）
       ctrlKey: false,
       shiftKey: false,
       altKey: false,
       metaKey: false,
       isAutoRepeat: false
     }
     ```
   - `event.preventDefault()` でブラウザのデフォルト動作を抑止
   - keydown と keyup の両イベントを送信

5. **Worker スレッド - キーボードイベント受信**：
   - `self.addEventListener('message', (event) => {...})`
   - `event.data.type === 'keyboardEvent'` で受信判定
   - `handleKeyboardEvent()` 関数を呼び出し

6. **Worker スレッド - イベント処理**：
   - `keydown` イベント時のみ処理（`keyup` は TBD）
   - イベント UUID をキーから対応する numeric window ID を逆引き
   - `key` プロパティが単一文字かつ ASCII 印字文字（0x20-0x7E）か判定
   - 有効な場合、文字の charCode（ASCIIコード）をイベントキューにエンキュー
   - コンソール出力でログ:
     ```
     [worker] Enqueued event code 97 (keydown - a) to window 1
     ```
   - 特殊キー（エンター、矢印キーなど）は現在スキップ

7. **将来の拡張**：
   - 特殊キーのコード割り当て（TBD）
   - `keyup` イベントの処理方法（TBD）
   - 修飾キー（Ctrl, Shift, Alt, Meta）の処理方法（TBD）

#### 修飾キーの例

- 単独キー: `key='a', code='KeyA'` → `"a"`
- Ctrl+A: `key='a', code='KeyA', ctrlKey=true` → `"Ctrl+a"`
- Shift+A: `key='A', code='KeyA', shiftKey=true` → `"Shift+A"`
- Ctrl+Shift+A: `ctrlKey=true, shiftKey=true` → `"Ctrl+Shift+A"`

#### 非 Canvas ウィンドウ時の挙動

- Terminal または FileManager がアクティブの場合、キーボードイベントは転送されない
- これらのウィンドウは独自のキーボードハンドラを持つため
- Canvas ウィンドウから他ウィンドウへの切り替え時、キーボード転送も自動的に停止

### 18.7 イベントキュー機能

#### イベントキューの概要

Worker は 1 つのグローバルなイベントキューを管理し、すべてのウィンドウが共有する。キーボードイベントを数値ベースで Rust タスク に提供する。

**ウィンドウのライフサイクル**:
- `js_open_window()` 呼び出し時に新規ウィンドウを登録（eventQueue は初期化されない）
- `js_close_window()` 呼び出し時にウィンドウを登録解除（eventQueue は変更されない）
- Worker 初期化時に eventQueue を空の配列で初期化（全ウィンドウで共有）

**イベントコード形式**:
- **文字キー**（`keydown` イベント）: ASCIIコード（0x20-0x7E）
  - 例: `'a'` → 97, `'A'` → 65, `'Z'` → 90, `' '` (スペース) → 32, `'0'` → 48
  - Main が `keydown` イベント受信時に `key` プロパティから判定
  - Worker が charCode 変換して enqueue
- **特殊キー**（矢印、Esc、Enter など）: TBD（予約済み、未定義）
  - 将来的に数値コード割り当て予定
  - 現在は処理対象外（キューにエンキューされない）
- **キューが空**: Rust 呼び出し時に 0 を返却

#### イベントキューへのアクセス

Rust タスクが `js_get_keyboard_event(window_id)` FFI 関数を呼び出すことで、対象ウィンドウのイベントキューから次のイベントコードをデキュー（取り出す）。

```rust
// Rust 側の使用例
let event_code = js_get_keyboard_event(window_id);
if event_code > 0 {
    let ch = event_code as u8 as char;  // ASCIIコード → 文字
    println!("User pressed: {}", ch);
} else {
    // キューが空、または特殊キー（未実装）
}
```

**呼び出しシーケンス**:

1. Main スレッドが `keydown` イベントをキャッチ
2. Canvas ウィンドウのアクティブ判定後、Worker に `keyboardEvent` メッセージ送信
3. Worker が `handleKeyboardEvent()` で:
   - イベント UUID から numeric window ID を逆引き
   - `key` が単一文字の ASCII 印字文字（0x20-0x7E）か判定
   - 有効な場合、charCode をイベントキューにエンキュー
4. Rust タスクが定期的に `js_get_keyboard_event(window_id)` を呼び出し
5. Worker が キューから次のイベントコードをデキューして返却
6. Rust タスクがコードを処理（表示、入力処理など）

**マルチウィンドウ環境**:
- 各ウィンドウのキー入力がすべて同じグローバルキューに入る
- Rust タスク A が `js_get_keyboard_event(windowId1)` を呼び出し
- Rust タスク B が `js_get_keyboard_event(windowId2)` を呼び出し
- 両者とも同じグローバルキューから順序通りデキュー
- `js_get_keyboard_event()` の windowId パラメータはキューを特定するために使われない（互換性のため受け付けるのみ）

#### スレッドセーフティ

- **イベントキュー操作**は Worker スレッド内のみで実行
- Main スレッドからアクセスなし
- すべてのウィンドウが同じキューを共有（ウィンドウID無関係）
- Worker 内での Rust FFI 呼び出しから逐次アクセス
- 並行アクセス問題なし（Worker は単一スレッド）

### 18.8 Rust 側のコンテキスト（OsContext）

**OsContext 構造体:**
```rust
pub struct OsContext {
    pub title_bar_height: u32,
}
```

**役割:**
- Rust アプリケーション内でシステム定数や UI パラメータを保持
- ウィンドウ作成時にレイアウト計算に使用可能

**パラメータ詳細:**
- `title_bar_height`: Main UI で定義された `TITLE_BAR_HEIGHT` 定数値（通常 32px）
  - Worker 起動時に `startWithCommand` メッセージで Main から渡される
  - `run_task(...)` 経由で Rust へ伝達される
  - Rust アプリケーションがウィンドウ内容領域をレイアウト計算する際に利用可能
  - 例：コンテンツ描画領域の Y オフセット計算時に使用

**使用例:**
```rust
let context = OsContext { title_bar_height: 32 };
let window = HariWindow::new(&context, "My App", Size::new(320, 152));
// title_bar_height を参考にして、ウィンドウ内の描画領域をレイアウト
let content_y_offset = context.title_bar_height as i32;
```

## 15. タスクバーウィンドウ一覧機能

### 15.1 目的

タスクバーに開いているウィンドウの一覧を表示し、ウィンドウの切り替えと管理を効率化する機能。

### 15.2 実装方針

**ウィンドウグループ化の計算処理**:
- ウィンドウ一覧の変化（作成・削除・タイトル変更）を監視
- ウィンドウタイプごとにグループ化情報を再計算
- グループ化ロジック：
  ```
  function groupWindowsByKind(windows: WindowModel[]): WindowGroupInfo[] {
    // 1. canvas タイプは個別表示（グループ化しない）
    // 2. terminal, filemanager, about, textviewer は同じ種類ごとにグループ化
    // 3. 各グループの windowIds は作成順（zIndex 順）に保持
    // 4. 新規グループの isExpanded は初期値 false
    // 5. グループが空になった場合は削除
  }
  ```

**アイコン対応表**:
- `terminal` → `terminal-2.svg`
- `filemanager` → `folder.svg`
- `about` → `category.svg`
- `textviewer` → `file-text.svg`
- `canvas` → `app-window.svg`（タスク定義に応じて変更可能）

**ボタンスタイル**:
- アクティブウィンドウ: 背景を青色 (#0b5ed7)、文字を白 (#fff)
- 非アクティブウィンドウ: 背景をデフォルト、文字を黒 (#111)
- ホバー時: 背景を薄い灰色 (#e9ecef) で表示

### 15.3 実装対象ファイル

- `src/main.ts`
  - ウィンドウグループ化ロジック実装
  - グループ化情報の状態管理
  - タスクバーボタンのイベントハンドラー（クリック・ホバー）
  - ドロップダウンメニュー表示/非表示制御

- `src/style.css`
  - タスクバー中央領域のレイアウト (flexbox)
  - ウィンドウボタンのスタイル（サイズ、配色、ホバー効果）
  - ドロップダウンメニューのスタイル（背景、テキスト、アニメーション）
  - グループボタンと個別ボタンの区別表示

### 15.4 状態管理に関わる規則

**ウィンドウ作成時**:
- 新規ウィンドウを `windows` 配列に追加
- `windowGroups` を再計算
- 新規グループが作成される場合は `isExpanded: false` で初期化
- 既存グループにウィンドウが追加される場合、グループの `windowIds` を更新

**ウィンドウ削除時**:
- 対象ウィンドウを `windows` 配列から削除
- `windowGroups` を再計算
- グループが空になった場合、そのグループを `windowGroups` から削除

**ウィンドウタイトル変更時**:
- `windows` 内の対象ウィンドウモデルを更新
- `windowGroups` の再計算は実行しない（グループ化に影響しないため）
- UI 再レンダリングで新タイトルが反映

**ウィンドウアクティブ状態変化時**:
- `activeWindowId` を更新
- タスクバーボタンのスタイル更新（既存実装と同じ）
- グループボタンは子ウィンドウがアクティブな場合、グループボタン自体もハイライト表示

**グループドロップダウン展開状態**:
- グループボタンクリック時に対応 `WindowGroupInfo.isExpanded` をトグル
- ドロップダウン外をクリック時、すべてのグループを自動クローズ（`isExpanded: false`）
- グループ内の個別ウィンドウをクリック時、クリック後に自動クローズ

### 15.5 実装の流れ

1. `WindowGroupInfo` 型定義を `src/main.ts` に追加
2. `groupWindowsByKind()` 関数を実装
3. `AppState` に `windowGroups` フィールドを追加し、初期化時に計算
4. ウィンドウ操作（作成・削除）時に `windowGroups` 再計算ロジックを組み込み
5. タスクバー HTML マークアップを拡張（ハンバーガーメニューと時計の間にボタン領域を追加）
6. タスクバーボタンのレンダリング関数を実装
7. CSS でウィンドウボタンのスタイルとドロップダウンメニューのレイアウトを定義
8. グループボタン・個別ボタン・ドロップダウンメニューのイベントハンドラーを実装
9. テストで以下を確認：
   - 複数ウィンドウ作成時にボタンが正しく表示される
   - グループ化と個別表示が正しく処理される
   - グループボタンのホバー・クリック時にドロップダウンが表示/非表示される
   - ドロップダウン内のウィンドウをクリックするとそのウィンドウが前面化される
   - ウィンドウ削除時にボタンが消える
   - ウィンドウタイトル変更時にボタンテキストが更新される

## 16. 将来の課題

### 16.1 スマホ対応

- **ターミナル表示の問題**
  - スマホではターミナルが見切れる
  - 対応方針: レスポンシブレイアウトの実装、ウィンドウサイズの最小化オプション、スクロール機能の改善

- **キー入力の問題**
  - アプリのキー入力に課題（物理キーボードなし環境での操作性）
  - 対応方針: オンスクリーンキーボードの実装、タッチジェスチャーのサポート、コマンド入力補助機能の追加
